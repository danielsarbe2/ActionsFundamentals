name: My GitHub Actions Demo
on:
  push:
    branches: [main]
    paths-ignore: [.github/**]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0,30 8-18 * * 1-5"
  workflow_dispatch:

jobs:
  SayHello:
    runs-on: ubuntu-latest
    steps:
      - run: | 
            echo "Hello All!"
      - name: Checkout
        uses: actions/checkout@v3.3.0           
